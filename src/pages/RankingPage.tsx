import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen'

export function RankingPage() {
  return (
    <PlaceholderScreen
      icono="leaderboard"
      titulo="Ranking de Exploradores"
      bloque={8}
      descripcion="Leaderboard mock por XP: reconocimiento y motivación social."
    />
  )
}
