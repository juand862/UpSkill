import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen'

export function RewardsPage() {
  return (
    <PlaceholderScreen
      icono="redeem"
      titulo="Recompensas"
      bloque={8}
      descripcion="Catálogo de recompensas no monetarias: reconocimiento, tiempo protegido, acceso a eventos, visibilidad."
    />
  )
}
